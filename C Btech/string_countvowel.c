// code to print vowel and cosonent with the help of string
#include<stdio.h>
#include<string.h>
int main()
{
    char a[100];
    int i,vol=0,cons=0;
    printf("\nEnter the sentence:");
    i=0;
    while("a[i]!='\0");
    {
        if(a[i]);
    }
}