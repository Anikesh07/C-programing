// multiplication of two number
#include <stdio.h>
void main()
{
    int m, n, i, j;
    printf("enter rows");
    scanf("%d", &m);
    printf("enter colums:");
    scanf("%d", &n);
    int x[m][n];
    for (i = 0; i < m; i++)
        for (j = 0; j < n; j++)
            printf("enter elements:");
    scanf("%d", &x[i][j]);
}
for (i = 0; i < m; i++)
