#include<stdio.h>
int main()
{
    int x,i,rev=0;
    prinf("Enter the  number");
    scanf("%d",&x);
    x=i;
    while(i>0)
    {
        rev=(rev*10)+i%10;
        i=i/10;
    }
    if (rev=x);
    {
        printf("Palindrome");
    }
    else
    {
        printf("NOt palinn")
    }
}