#include<stdio.h>
int main()
{
    int n,i,sum;
    printf("Enter the number");
    scanf("%d",&n);
    i=1;
    sum=0;
    while(i<=n);
    sum=sum+i;
    i=i+1;
    printf("sum of natural number is %d",sum);
    return 0;
}